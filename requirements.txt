# =============================================================================
# XAI506 Final Project — demo.py (오프사이드 SAM2 + VLM + TTS 데모)
#
# Python: 3.10–3.12 권장
#
#   conda create -n DL-final-term python=3.11 -y
#   conda activate DL-final-term
#   pip install -r requirements.txt
#
# NVIDIA GPU(CUDA) 휠: [PyTorch 시작하기](https://pytorch.org/get-started/locally/)
# 에서 torch/torchvision을 먼저(또는 덮어) 설치한 뒤 나머지를 설치하세요.
# =============================================================================

# --- PyTorch (Mac MPS / CPU / Linux 기본 PyPI 휠) ---
torch>=2.1.0
torchvision>=0.16.0

# --- Hugging Face (SAM2 + SmolVLM2 + Qwen3-TTS) ---
# qwen-tts는 transformers 5.x와 충돌할 수 있어 4.x로 고정합니다.
transformers>=4.50.0,<5.0.0
accelerate>=1.0.0
safetensors>=0.4.0
huggingface-hub>=0.20.0

# --- 수치 / 이미지 / 시각화 ---
numpy>=1.26.0,<3.0.0
pillow>=10.0.0
matplotlib>=3.8.0

# --- demo.py / sam2_helpers.py ---
scipy>=1.11.0          # 마스크 후처리 (binary_fill_holes, label)
PyQt5>=5.15.0          # 선수 클릭 픽커 (데스크톱 GUI 필요)

# --- SmolVLM2 (--explain / smolvlm_helpers.py) ---
num2words>=0.5.0

# --- Qwen3-TTS (tts_helpers.py) ---
qwen-tts>=0.1.0
soundfile>=0.12.0
torchaudio>=2.1.0
