# =============================================================================
# XAI506 Final Project — demo.py (오프사이드 SAM2 데모)
#
# Python: 3.10–3.12 권장
#
#   conda create -n DL-final-term python=3.11 -y
#   conda activate DL-final-term
#   pip install -r requirements.txt
#
# NVIDIA GPU(CUDA) 휠: [PyTorch 시작하기](https://pytorch.org/get-started/locally/)
# 에서 torch/torchvision을 먼저(또는 덮어) 설치한 뒤 나머지를 설치하세요.
# =============================================================================

# --- PyTorch (Mac MPS / CPU / Linux 기본 PyPI 휠) ---
torch>=2.1.0
torchvision>=0.16.0

# --- Hugging Face (SAM2: facebook/sam2.1-hiera-base-plus) ---
transformers>=4.50.0
accelerate>=1.0.0
safetensors>=0.4.0
huggingface-hub>=0.20.0

# --- 수치 / 이미지 / 시각화 ---
numpy>=1.26.0,<3.0.0
pillow>=10.0.0
matplotlib>=3.8.0

# --- demo.py / sam2_helpers.py ---
scipy>=1.11.0          # 마스크 후처리 (binary_fill_holes, label)
PyQt5>=5.15.0          # 선수·공 클릭 픽커 (데스크톱 GUI 필요)

# =============================================================================
# 선택 사항
# =============================================================================

# scripts/ 노트북(Jupyter) 실행 시
# notebook>=7.0.0
# ipykernel>=6.29.0

# 03_hf_smolvlm.ipynb
# num2words>=0.5.0

# 02_hf_depth_anything.ipynb 3D 시각화
# open3d>=0.18.0
